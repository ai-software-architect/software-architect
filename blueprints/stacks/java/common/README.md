# Java Common Stack Rules

This stack layer defines Java-wide guidance independent of framework.

## Main Guidance

- prefer explicit types and clear object responsibilities
- keep domain types free from framework annotations
- use package names that match architecture boundaries
- avoid generic technical naming in domain code
